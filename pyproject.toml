[project]
name = "personal-site"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "zensical>=0.0.2",
]

[dependency-groups]
dev = [
    "zensical>=0.0.2",
]
